#!/usr/bin/env node

const db = require('../db/config.js');

    console.log('scheduler task initiated');

    db.query(`INSERT INTO dummy.cron (var, numnum) VALUES ('bye', 100);`)
      .on('end', function() {
        console.log('check dummy yo');
      })
      .catch(console.error)
